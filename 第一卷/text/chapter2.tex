\chapter{一元函数}\label{chap:one_variable_functions}

\section{函数概念}\label{sec:function_concept}


\subsection{变量及其变动区域}\label{subsec:43}
在 \emref{subsec:22} 中, 我们已经介绍了变量的一般概念.
设数集 $\X=\{x\}$ 给出了变量 $x$ 在所考察问题中可能取得的全部数值,
并且其中每一个数值都允许取到,
这个数集就称为变量 $x$ 的\emph{变动区域}\index{biandongquyu@变动区域},
也就是变量的取值范围.
一般来说, 任意数集都可以作为某个变量的变动区域
\footnote{对数列而言, 仅给出所有可能的取值还不能确定这个数列,
因为各项的先后次序和重复情况同样是数列的组成部分
[参见 \emref{subsec:22}].}.

我们已经讲过, 数可以表示为数轴上的点, 因此数集 $\X$ 也可以看成数轴上的一个点集.
在这种几何解释下, 变量所取的数值通常也称为点.

例如, 变量 $n$ 可以以自然数集 $\N$ 为变动区域.
数列
$
x_n=\dfrac{1+(-1)^n}{n}
$
取得的不同数值则构成数集
\footnote{若把数列看成定义在 $\N$ 上的函数,
则应当区分它的定义域 $\N$ 与它实际取得的值域.
本书以后讨论数列时, 不再使用“数列的变动区域”这一说法
[参见 \emref{subsec:45}].}
$
\left\{0,1,\dfrac12,\dfrac13,\ldots\right\}.
$
常量也可以看成变量的特殊情形, 它的变动区域只含一个数.

分析学中通常考察连续变动的变量.
时间、运动点经过的路程等物理量, 都是这类变量的典型例子.
对这类变量而言, 最常用的变动区域是\emph{有限区间}\index{youxianqujian@有限区间},
它由两个实数 $a,b$ 作为为区间的端点或边界 (通常假设 $a<b$).
按照是否包含端点, 有限区间分为以下几种形式:
\begin{align*}
&\text{\emph{闭区间}}\index{biqujian@闭区间}
&&[a,b]=\{x:a\leqslant x\leqslant b\},\\
&\text{\emph{半开区间}}\index{bankaqujian@半开区间}
&&(a,b]=\{x:a<x\leqslant b\},\\
&&&[a,b)=\{x:a\leqslant x<b\},\\
&\text{\emph{开区间}}\index{kaiqujian@开区间}
&&(a,b)=\{x:a<x<b\}.
\end{align*}
在每一种情形中, $b-a$ 都称为区间的长度.
很容易想象, 在数轴上, 有限区间表示为一条线段;
区间是否包含端点, 对应于线段是否包含相应的端点.

有时还需要考察\emph{无穷区间}\index{wuqiongqujian@无穷区间}.
它们用“广义的数” $-\infty,+\infty$ 作为一端或两端, 记法与有限区间相似.
例如, $(-\infty,+\infty)$ 表示全体实数,
$(a,+\infty)$ 表示满足 $x>a$ 的全体实数,
$(-\infty,b]$ 表示满足 $x\leqslant b$ 的全体实数.
在数轴上, 无穷区间表示为向两端无限延伸的直线, 或向一端无限延伸的射线.

\subsection{变量间的函数关系，例题}\label{subsec:44}
数学分析主要研究的不是某一个变量的独立变化,
而是两个或多个变量同时变化时彼此之间的关系.
这里先考察两个变量的最简单情形.

在数学、物理学、工程学以及日常生活中,
我们经常遇到同时变动的两个量.
它们并不能各自任意取值:
相反地,
当其中一个变量 (\emph{自变量}\index{zibianliang@自变量}) 的值给定后,
另一个变量 (\emph{因变量}\index{yinbianliang@因变量}或\emph{函数}\index{hanshu@函数}) 的值也随之确定了.
下面先看几个例子.

\myrefparagraph{1)}\label{ex:circle_area_function}
圆的面积 $Q$ 是其半径 $R$ 的函数.
给定 $R$ 后, $Q$ 由公式
$$
Q=\pi R^2
$$
唯一确定.

\myrefparagraph{2)}\label{ex:free_fall_function}
忽略空气阻力时, 质点从静止开始自由落下.
从运动开始计时, 经过时间 $t$ 后的路程 $s$ 满足
$$
s=\frac{gt^2}{2},
$$
其中 $g=9.81\,\mathrm{m/s^2}$ 为重力加速度.
因此, 路程 $s$ 就成为经过的时间 $t$ 的函数.

\myrefparagraph{3)}\label{ex:boyle_mariotte_function}
一定质量的理想气体密闭在带活塞的气缸中.
温度保持不变时, 气体的体积 $V$ 与压力 $p$ 遵循 Boyle--Mariotte 定律: $pV=c=$ 常数.
于是给定体积 $V$ 后, 压力由
$$
p=\frac{c}{V}
$$
唯一确定, 因而 $p$ 是 $V$ 的函数.

\myrefparagraph{4)}\label{ex:barometric_function}
再考察大气压力 $p$ 与海拔高度 $h$ 的关系.
物理学给出气压公式
$$
p=p_0\e^{-kh},
$$
其中 $p_0$ 是海平面处的气压, $k$ 是常量.
若给定高度 $h$, 上式就把 $p$ 表示为 $h$ 的函数.

最后, 还应该有这个概念, 在两个相互关联的变量中, 选哪一个作为自变量并非总是固定的.
实际选择通常由研究目的和计算上的便利决定.
例如对于最后的例子, 一个飞行师若要根据气压计的读数 $p$ 来确定此时的飞行高度,
就应交换两个变量的角色, 将同一关系写成
$$
h=\frac{1}{k}\ln\frac{p_0}{p}.
$$
其它的例子也有类似的写法.
需要注意的是,
最开始的两个例子中, 我们交换两个变量后,
理论上对应于一个自变量, 有两个自变量 (一正一负) 随之确定,
这个时候, 我们还需要关注新的因变量 (长度 $R$ 与 时间 $t$) 的实际意义,
它们通常意义下只能选取正的情况.


\subsection{函数概念的定义}\label{subsec:45}
现在我们抽去上述各个量的具体物理意义, 来给出函数概念的一般定义.

{\kaishu 设 $\X,\Y$ 是两个给定的数集,
如果按照某一确定的对应法则 $f$,
对每一个 $x\in\X$, 都有唯一确定的 $y\in\Y$ 与之对应,
那么称 $f$ 是从 $\X$ 到 $\Y$ 的函数, 记作
$$
f:\X\to\Y,
$$
并写成 $y=f(x)$}.
自变量 $x$ 也称为函数的\emph{变元}\index{bianyuan@变元},
$y=f(x)$ 称为函数在 $x$ 处的\emph{函数值}\index{hanshuzhi@函数值}.

在这个定义中存在两个要素:
第一, 指出变元 $x$ 的变动区域 $\X$ (它称为函数 的\emph{定义域}\index{dingyiyu@定义域});
第二, 确定 $x$ 与 $y$ 的数值之间的对应法则或规律
(函数 $y$ 的变动区域 $\Y$ 通常并不指出, 因为对应的规律本身就已经确定函数值的集合了
\footnote{在现代集合论的语言中,
我们将 $\Y$ 称为函数的\emph{陪域}\index{peiyu@陪域},
所有实际函数值构成的集合 $f(\X)=\{f(x):x\in\X\}$ 称为函数的\emph{值域}\index{zhiyu@值域},
后者是 $\Y$ 的子集, 但未必等于 $\Y$.
例如, $f$ 是恒等于零的一个函数, 那么它的值域由唯一的零构成, 但是其变动区域则可以是任意一个含有零的集合.
}).

按照定义, 每个 $x\in\X$ 只能对应一个函数值.
但是我们也可以建立更为普遍的观点上, 就是假设同一个 $x$ 允许对应多个 $y$,
在这种场合函数就称为\emph{多值函数}\index{duozhihanshu@多值函数},
用以区分前面所定义的\emph{单值函数}\index{danzhihanshu@单值函数}.
然而, 在分析学中, 如果我们的变量都取值于实数, 大多不讨论多值函数的情况,
以后说到函数, 如无特别说明, 我们始终理解成单值函数.

我们通常用不同的字母 $f,\varphi,F,\ldots$ 表示不同的对应法则
如果同时考察具有同一自变量的几个不同函数,
就应当用不同的字母区分它们:
$$
y=f(x),\qquad y=\varphi(x),\qquad y=F(x),\quad\ldots.
$$
有时我们就直接重复用着字母 $y$, 记成 $y=y(x)$.

某些情形下, 也会把自变量写成下标,
数列的记号 $x_n$ 就属于这种形式, 它是自变量 $n$ 的函数, 而 $n$ 则取值于自然数集 $\N$.
类似地 $N_\epsilon$ 的记法 [\emref{subsec:23}] 表示序号 $N$ 依赖着 $\epsilon$, 等等.

若要指出函数 $f$ 对应于某个特定自变量值 $x_0$ 的函数值,
就写成 $f(x_0)$.
例如, 若
$$
f(x)=\frac{1}{1+x^2},\qquad
g(t)=\frac{10}{t},\qquad
h(u)=\sqrt{1-u^2},
$$
则 $f(1)$ 表示 $f(x)$ 在 $x=1$ 时的函数值, 即化简后的数 $\dfrac{1}{2}$,
类似地, $g(5)=2, h\brac{\dfrac35}=\dfrac45$ 等等.

函数对应法则的给出方式可以多种多样.
最简单也最常用的方式,
是用\emph{解析式}\index{jiexishi@解析式}或\emph{公式}\index{gongshi@公式}说明应对 $x$ 作哪些运算才能得到 $y$.
这种\emph{解析表示法}\index{jiexibiaoshifa@解析表示法}是数学分析中最重要的方法 (我们将在下一小节讨论),
我们在 \emref{subsec:44} 中讲述的四个例题都采用了这种解析表示.

然而, 解析式并不是表示函数的唯一方法, 在数学内也有很多函数是无法用公式来定义的.
例如, 常用的取整函数 $\floor{x}$
\footnote{参见 \emref{subsec:25} 中例 \ref{ex:infinitesimal_basic_sequences} 的脚注.},
它就无法给出一个单一的算式.

还有许多只在自然数上取值的“算术函数,
例如阶乘 $n!=1\cdot2\cdots n$,
正因数个数函数 $\tau(n)$,
以及表示 $1,2,\ldots,n$ 中与 $n$ 互素的整数个数的 Euler 函数 $\varphi(n)$.

在自然科学和工程学中, 变量间的函数关系还常由实验或观测确定.
例如, 对任意选定的大气压力 $p$, 实验都可测得与之对应的水的沸点温度 $\theta$;
因此 $\theta$ 是 $p$ 的函数.
这种关系未必由一个公式表示, 也可以用实验数据表给出.
这种\emph{列表法}\index{liebiaofa@列表法}给定函数的实例在任何工程手册上都很容易找到.

最后, 函数关系还可以直接用\emph{图像}\index{tuxiang@图像}表示.
例如, 蒸汽机的指示器所画出的指示图,
表示汽缸内蒸汽压力 $p$ 与体积 $V$ 之间的关系;
气压自记器得到的气压图, 则表示大气压力在昼夜间的变化.
函数的表格表示和图形表示在分析中并非始终必须,
因而这里不再详细展开.

\subsection{函数的解析表示法}\label{subsec:46}
函数的解析式或公式的表示法在数学分析中起着极其重要的作用.
下面先对这种表示法作几点说明.

\myrefparagraph{1\textdegree}\label{par:analytic_operations}
首先要说明, 解析式中允许进行哪些运算.
除初等代数和三角学中已经研究过的算术运算、乘幂、开方、取对数、求三角函数值及其反运算外,
还要加入我们在第一章中引入的一种新的运算 —— 极限运算.
随着分析学内容的展开, 还会逐步引入新的运算,
因此“解析式”或“公式”的完整含义也只能逐步揭示.

\myrefparagraph{2\textdegree}\label{par:natural_domain}
其次要注意解析式所表示的函数的定义域.
每一个含有自变量 $x$ 的解析式都有一个自然定义域,
即使这个解析式具有意义的一切实数 $x$ 所组成的集合
\footnote{对于任何实数 $x$ 都没有意义的解析式, 这里不加讨论.}.

例如, 表达式 $\dfrac{1}{1+x^2}$ 的自然定义域是全体实数 $\R$;
$\sqrt{1-x^2}$ 的自然定义域是闭区间 $[-1,1]$,
而 $\dfrac{1}{\sqrt{1-x^2}}$ 的自然定义域是开区间 $(-1,1)$.
有时自然定义域由几个彼此分离的区间组成:
$\sqrt{x^2-1}$ 的自然定义域是 $(-\infty,-1]$ 和 $[1,+\infty)$,
而 $\dfrac{1}{x^2-1}$ 的自然定义域则是 $(-\infty,-1)$, $(-1,1)$ 和 $(1,+\infty)$.

再考察无穷几何级数的和
$$
1+x+x^2+\cdots
=\lim_{n\to\infty}\brac{1+x+x^2+\cdots+x^{n-1}}
$$
若 $\abs{x}<1$, 则我们知道 [\emref{subsec:25}, \ref{ex:geometric_series_sum}],
它有极限 $\dfrac{1}{1-x}$,
而当 $\abs{x}\geqslant1$ 时, 上式中的极限或者等于 $+\infty$, 或者根本不存在.
因此这个解析式的自然定义域是开区间 $(-1,1)$.

以后我们还会遇到更复杂、更一般的解析式,
并研究它们在自然定义域内所确定的函数的性质, 即研究解析工具本身.
不过, 在具体问题中还可能出现另一种情形:
自变量 $x$ 由于问题本身的性质而被限制在某个变动区域 $\X$ 内,
即使解析式在 $\X$ 以外仍有意义, 也不能越出这个区域来讨论原问题.
例如, 在自由落体问题 [\emref{subsec:44}, \ref{ex:free_fall_function}] 中,
质点从地面以上高 $h$ 处开始下落, 路程由公式
$$
s=\frac{gt^2}{2}
$$
给出. 这个解析式对于一切实数 $t$ 都有意义,
但原问题只允许考察
$$
0\leqslant t\leqslant T,\qquad T=\sqrt{\frac{2h}{g}}.
$$
负的时间没有实际意义; 当 $t>T$ 时, 质点已经落到地面.
在这里, 解析式只起辅助作用, 函数实际考察的定义域由具体问题限定.

\myrefparagraph{3\textdegree}\label{par:piecewise_analytic_representation}
最后, 函数未必在整个定义域内由同一个公式给出,
而是对于变元的某一部分数值用某一公式，对于另一部分数值用另一公式
例如, 在区间 $(-\infty,+\infty)$ 上定义函数
$$
f(x)=
\begin{cases}
1, & \abs{x}>1,\\
-1, & \abs{x}<1,\\
0, & x=\pm1.
\end{cases}
$$
这里对自变量的不同部分分别使用了不同的公式.

又如, \emph{Dirichlet 函数}\index{Dirichlethanshu@Dirichlet 函数}定义为
$$
D(x)=
\begin{cases}
1, & x\text{ 是有理数},\\
0, & x\text{ 是无理数}.
\end{cases}
$$
再如, \emph{符号函数}\index{fuhaohanshu@符号函数}也称为 Kronecker 函数, 记作 $\sgn x$:
$$
\sgn x=
\begin{cases}
1, & x>0,\\
-1, & x<0,\\
0, & x=0.
\end{cases}
$$

不过, 由几个公式分段定义的函数与由单一公式定义的函数之间并没有原则上的差别.
通常, 借助较为复杂的表达式, 可以把分段定义改写成一个公式.
例如, 利用极限运算, 上面引入的 $f(x)$ 可以写成
$$
f(x)=\lim_{n\to\infty}\frac{x^{2n}-1}{x^{2n}+1}.
$$
很容易检验, 当 $\abs{x}>1$ 时, $x^{2n}\to+\infty$, 因而
$$
\lim_{n\to\infty}\frac{x^{2n}-1}{x^{2n}+1}
=\lim_{n\to\infty}\frac{1-\dfrac{1}{x^{2n}}}{1+\dfrac{1}{x^{2n}}}=1.
$$
当 $\abs{x}<1$ 时, $x^{2n}\to0$, 所以
$$
\lim_{n\to\infty}\frac{x^{2n}-1}{x^{2n}+1}=-1.
$$
最后, 当 $x=\pm1$ 时, 对每个 $n$ 都有
$$
\frac{x^{2n}-1}{x^{2n}+1}=0,
$$
取极限后仍得 $0$. 这正好与原来的分段定义完全一致.

\subsection{函数的图像}\label{subsec:47}
虽然在数学分析中并不用图像来给出函数,
但经常要借助图像说明函数的性质.
图像直观而清楚, 是研究函数性质时不可缺少的辅助工具.

\begin{wrapfigure}[7]{l}{0.34\textwidth}
    \vspace{-0.3cm}
    \centering
    \begin{tikzpicture}
        \draw[-latex] (-0.5, 0) -- (3.2, 0) node[below,scale=0.8] {$x$};
        \draw[-latex] (0, -0.2) -- (0, 2.5) node[right,scale=0.8] {$y$};
        \node[below left,scale=0.8] (O) at (0, 0) {$O$};

        \node[below left,scale=0.8] (A) at (0.5, 0.7055) {$A$};
        \node[above,scale=0.8] (B) at (3, 1.75) {$B$};
        \node[below right,scale=0.8] (M) at (1.37, 1.2629) {$M$};

        \draw[domain=0.3:3, thick] plot (\x, {ln(\x+1)+0.4});

        \draw[dashed,thick] (0.76, 0) -- (0.76, 0.9653);
        \draw[dashed,thick] (1.37, 0) -- (1.37, 1.2629);
        \draw[dashed,thick] (2.26, 0) -- (2.26, 1.5817);
        \draw[dashed,thick] (0, 0.9653) -- (0.76, 0.9653);
        \draw[dashed,thick] (0, 1.2629) -- (1.37, 1.2629);
        \draw[dashed,thick] (0, 1.5817) -- (2.26, 1.5817);

        \draw[fill=white] (0.76, 0.9653) circle (1.5pt);
        \draw[fill=white] (1.37, 1.2629) circle (1.5pt);
        \draw[fill=white] (2.26, 1.5817) circle (1.5pt);
    \end{tikzpicture}
 \end{wrapfigure}
设函数 $y=f(x)$ 定义在某个区间 $\X$ 内.
在平面上取两条互相垂直的坐标轴, 对每一对满足 $x\in\X$、$y=f(x)$ 的数值,
作横坐标为 $x$、纵坐标为 $y$ 的点 $M(x,y)$.
当 $x$ 在 $\X$ 内变动时, 所有这些点组成的集合就是函数 $y=f(x)$ 的\emph{图像}.
在图示的情形中, 这些点画出一条曲线 $AB$;
这条曲线是函数的几何表示, 而方程 $y=f(x)$ 就称为曲线 $AB$ 的方程.

例如, 函数
$$
\begin{aligned}
y=\pm\sqrt{1-x^2}\quad (\abs{x}\leqslant1),\qquad
y=\pm\sqrt{x^2-1}\quad (\abs{x}\geqslant1)
\end{aligned}
$$
的图像分别是下方圆和等轴双曲线:

\begin{figure}[htbp]
    \vspace{-0.7cm}
    \centering
    \begin{minipage}[t]{0.44\textwidth}
        \centering
        \begin{tikzpicture}[scale=1.45]
            \draw[-latex] (-1.45,0) -- (1.5,0) node[right,scale=0.8] {$x$};
            \draw[-latex] (0,-1.35) -- (0,1.45) node[right,scale=0.8] {$y$};
            \node[below left,scale=0.8] at (0,0) {$O$};
            \draw[thick] (1,0) arc[start angle=0,end angle=180,radius=1];
            \draw[dashed,thick] (-1,0) arc[start angle=180,end angle=360,radius=1];
            \fill (-1,0) circle (1.2pt);
            \fill (1,0) circle (1.2pt);
            \node[above right,scale=0.72] at (0.35,0.9) {$y=+\sqrt{1-x^2}$};
            \node[below right,scale=0.72] at (0.35,-0.9) {$y=-\sqrt{1-x^2}$};
            \node[below left,scale=0.8] at (-1,0) {$-1$};
            \node[below right,scale=0.8] at (1,0) {$1$};
        \end{tikzpicture}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \begin{tikzpicture}[scale=0.95]
            \draw[-latex] (-2.65,0) -- (2.7,0) node[right,scale=0.8] {$x$};
            \draw[-latex] (0,-2.25) -- (0,2.35) node[right,scale=0.8] {$y$};
            \node[below left,scale=0.8] at (0,0) {$O$};
            \draw[dashed] (-2.2,-2.2) -- (2.2,2.2);
            \draw[dashed] (-2.2,2.2) -- (2.2,-2.2);
            \draw[domain=1:2.45,samples=100,thick] plot (\x,{sqrt(\x*\x-1)});
            \draw[domain=1:2.45,samples=100,thick] plot (\x,{-sqrt(\x*\x-1)});
            \draw[domain=-2.45:-1,samples=100,thick] plot (\x,{sqrt(\x*\x-1)});
            \draw[domain=-2.45:-1,samples=100,thick] plot (\x,{-sqrt(\x*\x-1)});
            \fill (-1,0) circle (1.5pt);
            \fill (1,0) circle (1.5pt);
            \node[above right,scale=0.72] at (1.35,0.7) {$y=+\sqrt{x^2-1}$};
            \node[below right,scale=0.72] at (1.35,-0.7) {$y=-\sqrt{x^2-1}$};
            \node[below left,scale=0.8] at (-1,0) {$-1$};
            \node[below right,scale=0.8] at (1,0) {$1$};
        \end{tikzpicture}
    \end{minipage}
\end{figure}

\begin{wrapfigure}[10]{r}{0.45\textwidth}
    \vspace{-0.5cm}
    \centering
    \begin{tikzpicture}[scale=0.85]
        \draw[-latex] (-2.65,0) -- (3.55,0) node[right,scale=0.8] {$x$};
        \draw[-latex] (0,-2.65) -- (0,2.85) node[right,scale=0.8] {$y$};
        \node[below left,scale=0.8] at (0,0) {$O$};
        \foreach \x in {-2,-1,1,2,3}{
            \draw (\x,0.08) -- (\x,-0.08) node[below] {$\x$};
        }
        \foreach \k in {-2,-1,0,1,2}{
            \draw[thick] (\k,\k) -- (\k+1,\k);
            \fill (\k,\k) circle (1.8pt);
            \draw[fill=white] (\k+1,\k) circle (1.8pt);
        }
        \node[left,scale=0.8] at (0,-2) {$-2$};
        \node[left,scale=0.8] at (0,-1) {$-1$};
        \node[left,scale=0.8] at (0,1) {$1$};
        \node[left,scale=0.8] at (0,2) {$2$};
        \node[above left,scale=0.8] at (-0.25,2.15) {$y=\floor{x}$};
    \end{tikzpicture}
\end{wrapfigure}
函数图像通常是逐点画出的.
在区间 $\X$ 内取一系列彼此接近的自变量值,
依公式 $y=f(x)$ 算出相应的函数值:
$$
\begin{array}{c|ccccc}
x= & x_1 & x_2 & x_3 & \cdots & x_n\\ \hline
y= & y_1 & y_2 & y_3 & \cdots & y_n
\end{array}
$$
再把点
$$
(x_1,y_1),\ (x_2,y_2),\ \ldots,\ (x_n,y_n)
$$
画在图上, 用光滑的线段连接作出曲线.
这样得到的曲线虽然只是所求图像的近似,
但是曲线画得越光滑, 所取的点越稠密, 它就越准确地表示函数的图像.

必须注意, 虽然常用几何图形来表示函数,
函数的图像却不一定是通常直观意义下的曲线.
例如, 取整函数 $y=\floor{x}$ 在每个区间 $[m,m+1)$ ($m$ 取一切整数) 内都保持常值 $m$,
因而它的图像由一系列左端点属于图像、右端点不属于图像的水平线段组成.
至于 Dirichlet 函数 $D(x)$,
它的图像由直线 $y=1$ 上横坐标为有理数的点,
以及 $x$ 轴上横坐标为无理数的点组成, 这种图像却无法实际画出.

\subsection{几类最重要的函数}\label{subsec:48}
这里列举几类最重要的函数, 它们通常称为\emph{初等函数}\index{chudenghanshu@初等函数}.

\myrefparagraph{1\textdegree}\label{par:rational_functions}
\emph{有理函数}\index{youlihanshu@有理函数}包括以下两类.
形如
$$
y=a_0x^n+a_1x^{n-1}+\cdots+a_{n-1}x+a_n,
\qquad a_0,a_1,\ldots,a_n\text{ 为常数},
$$
的函数称为\emph{有理整函数}. 两个这样的多项式之比
$$
y=\frac{a_0x^n+a_1x^{n-1}+\cdots+a_{n-1}x+a_n}
{b_0x^m+b_1x^{m-1}+\cdots+b_{m-1}x+b_m}
$$
称为\emph{有理分式函数}. 它对于除去使分母为零的 $x$ 以外的一切数值都有意义.

例如, 下图给出了函数 $y=ax^2$ 在系数 $a$ 取各种不同数值时的图像 (抛物线),
以及函数 $y=\dfrac{a}{x}$ 在系数 $a$ 取各种不同数值时的图像 (等轴双曲线).
\begin{figure}[htbp]
    \vspace{-0.5cm}
    \centering
    \begin{minipage}[t]{0.45\textwidth}
        \centering
        \begin{tikzpicture}
            \draw[-latex] (-2.5, 0) -- (2.5, 0) node[below,scale=0.8] {$x$};
            \draw[-latex] (0, -0.2) -- (0, 4.2) node[above,scale=0.8] {$y$};
            \node[below left] (O) at (0, 0) {$O$};

            \draw[domain=-2.5:2.5, thick] plot (\x, {0.125 * \x*\x});
            \draw[domain=-2.5:2.5, thick] plot (\x, {0.25 * \x*\x});
            \draw[domain=-2.5:2.5, thick] plot (\x, {0.5 * \x*\x});
            \draw[domain=-2:2, thick] plot (\x, {1 * \x*\x});
            \draw[domain=-1.414:1.414, thick] plot (\x, {2 * \x*\x});
            \draw[domain=-1:1, thick] plot (\x, {4 * \x*\x});
            \draw[domain=-0.707:0.707, thick] plot (\x, {8 * \x*\x});

            \node[right,scale=0.8] (A) at (2.5, 0.125*2.5*2.5) {$\dfrac{1}{8}$};
            \node[right,scale=0.8] (B) at (2.5, 0.25*2.5*2.5) {$\dfrac{1}{4}$};
            \node[right,scale=0.8] (C) at (2.5, 0.5*2.5*2.5) {$\dfrac{1}{2}$};
            \node[right,scale=0.8] (D) at (2, 1*2*2) {$1$};
            \node[above,scale=0.8] (E) at (1.414, 4) {$2$};
            \node[above,scale=0.8] (F) at (1, 4) {$4$};
            \node[above,scale=0.8] (G) at (0.707, 4) {$8$};
            \node[above,scale=0.8] (f) at (-2, 4) {$y=ax^2$};
        \end{tikzpicture}
    \end{minipage}
    \begin{minipage}
        [t]{0.45\textwidth}
        \centering
        \begin{tikzpicture}
            \draw[->] (-2.1, 0) -- (2.1, 0) node[below,scale=0.8] {$x$};
            \draw[->] (0, -2.1) -- (0, 2.1) node[above,scale=0.8] {$y$};
            \node[below left,scale=0.8] (O) at (0, 0) {$O$};

            \draw[domain=-2:-0.5, thick] plot (\x, {1/\x});
            \draw[domain=0.5:2, thick] plot (\x, {1/\x});
            \draw[domain=-2:-0.25, thick] plot (\x, {1/(2*\x)});
            \draw[domain=0.25:2, thick] plot (\x, {1/(2*\x)});
            \draw[domain=-2:-1, thick] plot (\x, {2/\x});
            \draw[domain=1:2, thick] plot (\x, {2/\x});
            \draw[dashed, domain=-2:-0.5, thick] plot (\x, {-1/\x});
            \draw[dashed, domain=0.5:2, thick] plot (\x, {-1/\x});
            \draw[dashed, domain=-2:-0.25, thick] plot (\x, {-1/(2*\x)});
            \draw[dashed, domain=0.25:2, thick] plot (\x, {-1/(2*\x)});
            \draw[dashed, domain=-2:-1, thick] plot (\x, {-2/\x});
            \draw[dashed, domain=1:2, thick] plot (\x, {-2/\x});

            \node[above right,scale=0.8] (A) at (1, 1) {$1$};
            \node[above left,scale=0.8] (B) at (-1, 1) {$-1$};
            \node[below left,scale=0.8] (C) at (0.707, 0.707) {$\frac{1}{2}$};
            \node[below right,scale=0.8] (D) at (-0.707, 0.707) {$-\frac{1}{2}$};
            \node[above right,scale=0.8] (E) at (1.414, 1.414) {$2$};
            \node[above left,scale=0.8] (F) at (-1.414, 1.414) {$-2$};
            \node[above,scale=0.8] (f) at (-1, 2) {$y = \dfrac{a}{x}$};
        \end{tikzpicture}
    \end{minipage}
\end{figure}

\myrefparagraph{2\textdegree}\label{par:power_functions}
形如
$$
y=x^\mu
$$
的函数称为\emph{幂函数}\index{mihanshu@幂函数}, 其中 $\mu$ 是任意实数.
当 $\mu$ 是整数时, 幂函数就是有理函数.
当 $\mu$ 是分数时, 得到根式. 例如, 若 $m$ 是自然数, 则
$$
y=x^{1/m}=\sqrt[m]{x}.
$$
若 $m$ 是奇数, 这个函数对于一切实数 $x$ 都有意义;
若 $m$ 是偶数, 并且只考虑根式的算术值, 则它只对 $x\geqslant0$ 有意义.
最后, 若 $\mu$ 是无理数, 就必须假定 $x>0$;
只有在 $\mu>0$ 时, 才可以再允许 $x=0$.

下图给出了幂函数在 $\mu$ 取各种不同数值时的图像.
\begin{figure}[htbp]
    \vspace{-0.3cm}
    \centering
    \begin{tikzpicture}[scale=0.9]
        \draw[-latex] (-0.2, 0) -- (3.2, 0) node[below,scale=0.8] {$x$};
        \draw[-latex] (0, -0.2) -- (0, 3.2) node[above,scale=0.8] {$y$};
        \node[below left] (O) at (0, 0) {$O$};
        \draw[dashed, domain=0:3] plot (\x, \x);

        \draw[domain=0:3, thick] plot (\x, {\x^(2/3)});
        \draw[domain=0:3, thick] plot (\x, {\x^(1/2)});
        \draw[domain=0:3, thick] plot (\x, {\x^(1/3)});
        \draw[domain=0:3, thick] plot (\x, {\x^(1/4)});
        \draw[domain=0:2.0801, thick] plot (\x, {\x^(3/2)});
        \draw[domain=0:1.7321, thick] plot (\x, {\x^(2)});
        \draw[domain=0:1.4422, thick] plot (\x, {\x^(3)});
        \draw[domain=0:1.3161, thick] plot (\x, {\x^(4)});

        \node[below right, scale=0.5] (A) at (3, 1.3161) {$1/4$};
        \node[right, scale=0.5] (B) at (3, 1.4422) {$1/3$};
        \node[right, scale=0.5] (C) at (3, 1.7321) {$1/2$};
        \node[right, scale=0.5] (D) at (3, 2.0801) {$3/2$};
        \node[above, scale=0.5] (E) at (1.7321, 3) {$2$};
        \node[above, scale=0.5] (F) at (1.4422, 3) {$3$};
        \node[above left, scale=0.5] (G) at (1.3161, 3) {$4$};
        \node[above right, scale=0.5] (D) at (2.0801, 3) {$2/3$};

        \node[scale=0.8] (f) at (0.6, 2.5) {$y = x^\mu$};
    \end{tikzpicture}
    \hfil
    \begin{tikzpicture}[scale=0.9]
        \draw[-latex] (-0.2, 0) -- (3.2, 0) node[below,scale=0.8] {$x$};
        \draw[-latex] (0, -0.2) -- (0, 3.2) node[above,scale=0.8] {$y$};
        \node[below left] (O) at (0, 0) {$O$};
        \draw[dashed, domain=0:3] plot (\x, \x);

        \draw[domain=0.1111:3, thick] plot (\x, {\x^(-1/2)});
        \draw[domain=0.3333:3, thick] plot (\x, {\x^(-1)});
        \draw[domain=0.5774:3, thick] plot (\x, {\x^(-2)});
        \draw[domain=0.6934:3, thick] plot (\x, {\x^(-3)});

        \node[right, scale=0.5] (A) at (3, 0.1111) {$-1/2$};
        \node[right, scale=0.5] (B) at (3, 0.3333) {$-1$};
        \node[right, scale=0.5] (C) at (3, 0.5774) {$-2$};
        \node[right, scale=0.5] (D) at (0.6934, 3) {$-3$};

        \node[scale=0.8] (f) at (2.5, 2.5) {$y = x^\mu$};
    \end{tikzpicture}
\end{figure}

\myrefparagraph{3\textdegree}\label{par:exponential_functions}
形如
$$
y=a^x
$$
的函数称为\emph{指数函数}\index{zhishuhanshu@指数函数},
其中 $a$ 是异于 $1$ 的正数, $x$ 可以取任意实数值.
下图给出了指数函数在 $a$ 取各种不同数值时的图像.

\myrefparagraph{4\textdegree}\label{par:logarithmic_functions}
形如
$$
y=\log_a x
$$
的函数称为\emph{对数函数}\index{duishuhanshu@对数函数},
其中 $a$ 同样是异于 $1$ 的正数, 而 $x$ 只能取正值.
下图给出了对数函数在 $a$ 取各种不同数值时的图像.

\begin{figure}[htbp]
    \vspace{-0.3cm}
    \centering
    \begin{tikzpicture}[scale=0.4]
        \draw[-latex] (-3.2, 0) -- (3.2, 0) node[below,scale=0.8] {$x$};
        \draw[-latex] (0, 0) -- (0, 9) node[above,scale=0.8] {$y$};
        \node[below,scale=0.8] (O) at (0, 0) {$O$};

        \draw[domain=-2.5:2.5, thick] plot (\x, {2^\x});
        \draw[domain=-2.5:1, thick] plot (\x, {8^\x});
        \draw[domain=-2.5:2.0794, thick] plot (\x, {exp(\x)});
        \draw[dashed, domain=-2.5:2.5, thick] plot (\x, {2^(-\x)});
        \draw[dashed, domain=-1:2.5, thick] plot (\x, {8^(-\x)});
        \draw[dashed, domain=-2.0794:2.5, thick] plot (\x, {exp(-\x)});

        \node[right,scale=0.8] (A) at (2.5, 2^2.5) {$2^x$};
        \node[above,scale=0.8] (B) at (1, 8) {$8^x$};
        \node[right,scale=0.8] (C) at (2.0794, 8) {$\e^x$};
        \node[left,scale=0.8] (D) at (-2.5, 5.6569) {$\brac{\dfrac{1}{2}}^x$};
        \node[left,scale=0.8] (E) at (-2.0794, 8) {$\brac{\dfrac{1}{\e}}^x$};
        \node[above,scale=0.8] (F) at (-1, 8) {$\brac{\dfrac{1}{8}}^x$};
    \end{tikzpicture}
    \hfil
    \begin{tikzpicture}[scale=0.6]
        \draw[-latex] (0, 0) -- (9, 0) node[below,scale=0.8] {$x$};
        \draw[-latex] (0, -2.7) -- (0, 2.7) node[above,scale=0.8] {$y$};
        \node[below left,scale=0.8] (O) at (0, 0) {$O$};

        \draw[domain=1:5.6569, thick] plot (\x, {ln(\x)/ln(2)});
        \draw[domain=1:8, thick] plot (\x, {ln(\x)/ln(8)});
        \draw[domain=1:8, thick] plot (\x, {ln(\x)});
        \draw[domain=0.1768:1, thick] plot (\x, {-ln(\x)/ln(1/2)});
        \draw[domain=0.125:1, thick] plot (\x, {-ln(\x)/ln(1/8)});
        \draw[domain=0.1353:1, thick] plot (\x, {-ln(\x)/ln(1/e)});
        \draw[dashed, domain=0.1768:5.6569, thick] plot (\x, {ln(\x)/ln(1/2)});
        \draw[dashed, domain=0.125:8, thick] plot (\x, {ln(\x)/ln(1/8)});
        \draw[dashed, domain=0.1353:8, thick] plot (\x, {ln(\x)/ln(1/e)});

        \node[right,scale=0.8] (A) at (5.6569, 2.5) {$\log_2 x$};
        \node[right,scale=0.8] (B) at (8, 1) {$\log_8 x$};
        \node[right,scale=0.8] (C) at (8, 2.0794) {$\ln x$};
        \node[right,scale=0.8] (D) at (5.6569, -2.5) {$\log_{\frac{1}{2}} x$};
        \node[right,scale=0.8] (E) at (8, -1) {$\log_{\frac{1}{8}} x$};
        \node[right,scale=0.8] (F) at (8, -2.0794) {$\log_{\frac{1}{e}} x$};
    \end{tikzpicture}
\end{figure}

\myrefparagraph{5\textdegree}\label{par:trigonometric_functions}
\emph{三角函数}\index{sanjiaohanshu@三角函数}包括
\begin{align*}
    &y=\sin x,\qquad y=\cos x,\\
    &y=\tan x,\qquad y=\cot x,\\
    &y=\sec x,\qquad y=\csc x.
\end{align*}
在没有特别说明时, {\kaishu 三角函数的变元作为角度来考察, 总是用弧度表示}.
对于 $\tan x$ 和 $\sec x$, 要从定义域中除去
$$
\frac{(2k+1)\pi}{2}\qquad(k \text{是整数})
$$
这些数值; 对于 $\cot x$ 和 $\csc x$, 则要除去
$$
k\pi\qquad(k \text{是整数}).
$$
下图给出了 $y=\sin x$、$y=\cos x$ 以及 $y=\tan x$、$y=\cot x$ 的图像.
正弦函数的图像通常称为正弦曲线.

\begin{figure}[htbp]
    \centering
    \begin{tikzpicture}
        \draw[-latex] (-1.8, 0) -- (8.1, 0) node[below,scale=0.8] {$x$};
        \draw[-latex] (0, -1.2) -- (0, 1.5) node[right,scale=0.8] {$y$};
        \node[below right,scale=0.8] (O) at (0, 0) {$O$};

        \draw[domain=-1.5708:7.8540, smooth, thick] plot (\x, {sin(\x r)});
        \draw[dashed, domain=-1.5708:7.8540, thick] plot (\x, {cos(\x r)});

        \node[below right,scale=0.8] (A) at (6.28, 0) {$\sin x$};
        \node[above left,scale=0.8] (B) at (4.7124, 0) {$\cos x$};
    \end{tikzpicture}
\end{figure}

\begin{figure}[!ht]
    \centering
    \begin{tikzpicture}[scale=0.62]
        \draw[-latex] (-1.8, 0) -- (8.1, 0) node[below,scale=0.8] {$x$};
        \draw[-latex] (0, -3.2) -- (0, 3.2) node[right,scale=0.8] {$y$};
        \node[above left,scale=0.8] (O) at (0, 0) {$O$};

        \draw[domain=-1.2490:1.2490, smooth, thick] plot (\x, {tan(\x r)});
        \draw[domain=1.8925:4.3906, smooth, thick] plot (\x, {tan(\x r)});
        \draw[domain=5.0342:7.5322, smooth, thick] plot (\x, {tan(\x r)});
        \draw[dashed, domain=-1.5708:-0.3218, thick] plot (\x, {cot(\x r)});
        \draw[dashed, domain=0.3218:2.8198, thick] plot (\x, {cot(\x r)});
        \draw[dashed, domain=3.4634:5.9614, thick] plot (\x, {cot(\x r)});
        \draw[dashed, domain=6.6050:7.8540, thick] plot (\x, {cot(\x r)});

        \node[below right,scale=0.8] (A) at (3.14, 0) {$\tan x$};
        \node[below left,scale=0.8] (B) at (1.5708, 0) {$\cot x$};
    \end{tikzpicture}
    \hfill
    \begin{tikzpicture}[scale=0.62]
        \draw[-latex] (-5, 0) -- (5, 0) node[below,scale=0.8] {$x$};
        \draw[-latex] (0, -3.2) -- (0, 3.2) node[right,scale=0.8] {$y$};
        \node[below right,scale=0.8] (O) at (0, 0) {$O$};

        \draw[domain=-1.2310:1.2310, smooth, thick] plot (\x, {sec(\x r)});
        \draw[domain=1.8746:4.3726, smooth, thick] plot (\x, {sec(\x r)});
        \draw[domain=-1.8746:-4.3726, smooth, thick] plot (\x, {sec(\x r)});
        \draw[dashed, domain=-4.7124:-3.4814, thick] plot (\x, {1/sin(\x r)});
        \draw[dashed, domain=-2.8018:-0.3398, thick] plot (\x, {1/sin(\x r)});
        \draw[dashed, domain=0.3398:2.8018, thick] plot (\x, {1/sin(\x r)});
        \draw[dashed, domain=4.7124:3.4814, thick] plot (\x, {1/sin(\x r)});

        \node[below left,scale=0.8] (A) at (0, 1) {$\sec x$};
        \node[below right,scale=0.8] (B) at (1.57, 1) {$\csc x$};
    \end{tikzpicture}
\end{figure}

另外, 特别是在工程问题中, 还有一类很有用的函数.

\myrefparagraph{6\textdegree}\label{par:hyperbolic_functions}
函数
\begin{gather*}
\sinh x=\frac{\e^x-\e^{-x}}{2},\qquad
\cosh x=\frac{\e^x+\e^{-x}}{2},\\
\tanh x=\frac{\sinh x}{\cosh x}
=\frac{\e^x-\e^{-x}}{\e^x+\e^{-x}},\qquad
\coth x=\frac{\cosh x}{\sinh x}
=\frac{\e^x+\e^{-x}}{\e^x-\e^{-x}}
\end{gather*}
分别称为\emph{双曲正弦}、\emph{双曲余弦}、\emph{双曲正切}和\emph{双曲余切},
统称为\emph{双曲函数}\index{shuangquhanshu@双曲函数}.
除双曲余切在 $x=0$ 时没有意义外, 这些函数对于一切实数 $x$ 都有意义.
它们与三角函数十分相似.

例如, 有下列公式 (注意其中的符号):
$$
\begin{aligned}
\cosh(x\pm y)&=\cosh x\cosh y\pm\sinh x\sinh y,\\
\sinh(x\pm y)&=\sinh x\cosh y\pm\cosh x\sinh y.
\end{aligned}
$$
由此, 令 $y=x$, 可得
$$
\cosh^2x-\sinh^2x=1,\qquad
\cosh2x=\cosh^2x+\sinh^2x,\qquad
\sinh2x=2\sinh x\cosh x.
$$
显然, 加法公式中的第一个等式可直接验证:
$$
\frac{\e^{x+y}+\e^{-x-y}}{2}
=\frac{\e^x+\e^{-x}}{2}\cdot\frac{\e^y+\e^{-y}}{2}
+\frac{\e^x-\e^{-x}}{2}\cdot\frac{\e^y-\e^{-y}}{2}.
$$
其余公式也可以同样地验证. 下图给出了双曲函数的图像.

\begin{figure}[!h]
    \centering
    \begin{tikzpicture}[scale=0.8]
        \draw[-latex] (-3, 0) -- (3, 0) node[below,scale=0.8] {$x$};
        \draw[-latex] (0, -2.2) -- (0, 3) node[right,scale=0.8] {$y$};
        \node[below right,scale=0.8] (O) at (0, 0) {$O$};

        \draw[domain=-1.7628:1.7628, thick] plot (\x, {(e^(\x)+e^(-\x))/2});
        \draw[domain=-1.4436:1.8185, thick] plot (\x, {(e^(\x)-e^(-\x))/2});

        \node[below left,scale=0.8] (A) at (0, 1) {$\cosh x$};
        \node[above right,scale=0.8] (B) at (0.3, 0) {$\sinh x$};
    \end{tikzpicture}
    \hfil
    \begin{tikzpicture}[scale=0.8]
        \draw[-latex] (-3, 0) -- (3, 0) node[below,scale=0.8] {$x$};
        \draw[-latex] (0, -2.7) -- (0, 2.7) node[right,scale=0.8] {$y$};
        \node[below right,scale=0.8] (O) at (0, 0) {$O$};

        \draw[domain=0.4237:2.5, thick]  plot (\x, {(e^(\x)+e^(-\x))/(e^(\x)-e^(-\x))});
        \draw[domain=-2.5:-0.4237, thick]  plot (\x, {(e^(\x)+e^(-\x))/(e^(\x)-e^(-\x))});
        \draw[domain=-2.5:2.5, thick]  plot (\x, {(e^(\x)-e^(-\x))/(e^(\x)+e^(-\x))});

        \node[above left,scale=0.8] (A) at (0, 0) {$\tanh x$};
        \node[above right,scale=0.8] (B) at (1, 1.313) {$\coth x$};
    \end{tikzpicture}
\end{figure}

\subsection{反函数的概念}\label{subsec:49}
在讨论反三角函数之前, 先对反函数作一个总的说明.

假设在某一个区域内给定函数 $y=f(x)$, 并设当 $x$ 在区域 $\X$ 变动时,
函数值所成的集合为 $\Y$ (实际情况中 $\X$ 和 $\Y$ 通常都是区间).
现在从 $\Y$ 中任取一个数值 $y=y_0$,
则在 $\X$ 中必能求出一个或几个数值 $x=x_0$, 使得
$$
f(x_0)=y_0.
$$
因此, $\Y$ 中的每个 $y$ 都与 $\X$ 中的一个或几个 $x$ 相对应.
那么我们将由此确定的、以 $\Y$ 为定义域的单值或多值函数 $x=g(y)$,
称为函数 $y=f(x)$ 的\emph{反函数}\index{fanhanshu@反函数}
\footnote{若函数 $f$ 的反函数是单值的, 就称函数 $f$ 是\emph{可逆的}.
通常谈到反函数时, 总是假定它是单值的.}.

下面考察几个例子.

\myrefparagraph{1)}\label{ex:inverse_exponential_function}
设 $y=a^x$ ($a>1$), 其中 $x$ 在区间 $(-\infty,+\infty)$ 内变动.
函数值 $y$ 充满区间 $(0,+\infty)$, 并且对于其中每个 $y$,
由对数的定义 [参见 \emref{subsec:20}] 可知, 只有一个确定的 $x=\log_a y$.
因此, 指数函数的反函数是单值的, 即对数函数.

\myrefparagraph{2)}\label{ex:inverse_square_function}
对于函数 $y=x^2$, 若 $x$ 在 $(-\infty,+\infty)$ 内变动,
则对应于值域 $[0,+\infty)$ 中的每个 $y$, 都有 $x=\pm\sqrt{y}$.
所以它的反函数是双值的. 通常把这个双值函数分成两个单值的支
$x=+\sqrt{y}$ 和 $x=-\sqrt{y}$.
若把原函数的定义域分别限制在 $[0,+\infty)$ 和 $(-\infty,0]$,
这两个单值支便分别成为函数 $y=x^2$ 的反函数
\footnote{关于反函数的存在性和单值性, 后文还会重新讨论.}.

\myrefparagraph{3)}\label{ex:inverse_hyperbolic_cosine}
类似的, 设 $y=\cosh x$, 其中 $x$ 仍在 $(-\infty,+\infty)$ 内变动.
由方程
$$
\frac{\e^x+\e^{-x}}{2}=y,
$$
得到关于 $\e^x$ 的二次方程
$$
\e^{2x}-2y\e^x+1=0.
$$
当 $y\geqslant1$ 时, 它给出两个数值
$$
\e^x=y\pm\sqrt{y^2-1},
$$
因而
$$
x=\ln\brac{y\pm\sqrt{y^2-1}}.
$$
这个反函数仍是双值的. 它分成两个单值支,
分别对应于 $x$ 从 $0$ 变到 $+\infty$, 以及从 $-\infty$ 变到 $0$.

\myrefparagraph{4)}\label{ex:inverse_hyperbolic_sine}
又若 $y=\sinh x$. 对任意实数 $y$, 我们只能解出一个正数值
$$
\e^x=y+\sqrt{y^2+1},
$$
因为另一个根 $y-\sqrt{y^2+1}$ 是负数, 必须舍去.
因此
$$
x=\ln\brac{y+\sqrt{y^2+1}},
$$
这里的反函数是单值的.

\begin{wrapfigure}[7]{r}{0.28\textwidth}
    \begin{tikzpicture}
        \draw[-latex] (-0.5, 0) -- (2.5, 0) node[below,scale=0.8] {$x$};
        \draw[-latex] (0, -0.5) -- (0, 2.5) node[right,scale=0.8] {$y$};
        \node[below left,scale=0.8] (O) at (0, 0) {$O$};
        \draw[domain=0:2.5] plot (\x, {\x});

        \draw[domain=0:2, thick] plot (\x, {sqrt(\x^2+1)});
        \draw[dashed, domain=1:2.23, thick] plot (\x, {sqrt(\x^2-1)});

        \draw[dashed,thick] (0.75,1.25)--(1.25,0.75);
        \draw[fill=white] (0.75,1.25) circle (1.5pt);
        \draw[fill=white] (1.25,0.75) circle (1.5pt);
    \end{tikzpicture}
\end{wrapfigure}
观察函数 $y=f(x)$ 的图像, 很容易判断它的反函数 $x=g(y)$ 是否为单值函数.
若任意一条平行于 $x$ 轴的直线与图像至多相交于一点, 反函数就是单值的;
若某条这样的直线与图像相交于两点或更多点, 反函数就是多值的.
在后一种情形中, 还可以按照图像把 $x$ 的变动区域分成几部分,
使每一部分分别对应一个单值支. 例如, 对抛物线 $y=x^2$,
只需分别考察它的右半支和左半支即可.

若 $x=g(y)$ 是 $y=f(x)$ 的反函数, 则这两个关系的图像本来就是重合的.
不过, 也可以仍用字母 $x$ 表示反函数的变元, 把反函数写成 $y=g(x)$.
这时坐标轴仍保持原来的位置, 而 $x$ 轴与 $y$ 轴所承担的角色互换了.
因此, $y=g(x)$ 的图像可由 $y=f(x)$ 的图像关于直线 $y=x$ 对称得到.
上面的指数函数与对数函数图像正是这样互相对应的;
幂函数图像关于第一象限角平分线的对称性也可由同一理由解释.

\subsection{反三角函数}\label{subsec:50}
作为 \emref{subsec:48} 中初等函数种类的补充, 现在再考察以下函数.

\myrefparagraph{7\textdegree}\label{par:inverse_trigonometric_functions}
\emph{反三角函数}\index{fansanjiaohanshu@反三角函数}包括
\begin{gather*}
y=\arcsin x,\qquad y=\arccos x,\qquad y=\arctan x,\\
y=\operatorname{arccot}x,\qquad y=\operatorname{arcsec}x,\qquad y=\operatorname{arccsc}x.
\end{gather*}
如同在 \emref{subsec:48} 的三角函数中所约定的那样,
反三角函数的值作为角或弧度来考察时, 也总是用弧度表示.

\begin{wrapfigure}{r}{0.25\textwidth}
    \vspace{-1.2cm}
    \begin{tikzpicture}
        \draw[-latex] (-1.5, 0) -- (1.5, 0) node[below,scale=0.8] {$x$};
        \draw[-latex] (0, -1.8) -- (0, 8) node[right,scale=0.8] {$y$};
        \node[above left,scale=0.8] (O) at (0, 0) {$O$};

        \draw[domain=-1:1, smooth, thick] plot (\x, {rad(asin(\x))});
        \draw[dashed, domain=-1:1, smooth, thick] plot (\x, {-rad(asin(\x))+ pi});
        \draw[dashed, domain=-1:1, smooth, thick] plot (\x, {rad(asin(\x))+ 2*pi});

        \draw[dashed] (1, pi/2) -- (0, pi/2);
        \draw[dashed] (-1, -pi/2) -- (0, -pi/2);
        \draw[dashed] (1, pi/2) -- (1, 0);
        \draw[dashed] (-1, -pi/2) -- (-1, 0);

        \node[below,scale=0.8] (A) at (1, 0) {$1$};
        \node[left,scale=0.8] (B) at (0, pi/2) {$\frac{\pi}{2}$};
        \node[above,scale=0.8] (C) at (-1, 0) {$-1$};
        \node[right,scale=0.8] (D) at (0, -pi/2) {$-\frac{\pi}{2}$};
    \end{tikzpicture}
\end{wrapfigure}
先考察反正弦函数. 函数 $y=\sin x$ 定义在 $(-\infty,+\infty)$ 上,
值域为 $[-1,1]$. 任意一条平行于 $x$ 轴并与正弦曲线相交的直线,
都有无穷多个交点. 换言之, 对于 $[-1,1]$ 中的每个 $y$,
都有无穷多个 $x$ 满足 $y=\sin x$. 因此它的反函数
$$
x=\operatorname{Arcsin}y
$$
是无穷多值的.

通常只考察正弦函数在闭区间
$\bbrac{-\dfrac{\pi}{2},\dfrac{\pi}{2}}$ 上的一个支.
在这个区间内, 对每个 $y\in[-1,1]$ 只有一个 $x$ 与之对应, 记作
$$
x=\arcsin y,
$$
并称为反正弦函数的\emph{主值}.
把正弦曲线关于第一象限角平分线对称, 就得到图中多值函数
$y=\operatorname{Arcsin}x$ 的图像; 其中主值支 $y=\arcsin x$ 用实线表示, 并满足
$$
-\frac{\pi}{2}\leqslant\arcsin x\leqslant\frac{\pi}{2}.
$$

由初等三角学可知, 反正弦函数的全部数值可写成
$$
\begin{aligned}
\operatorname{Arcsin}x&=\arcsin x+2k\pi,\\
\operatorname{Arcsin}x&=(2k+1)\pi-\arcsin x,
\qquad k\in\Z.
\end{aligned}
$$

再由正弦的加法公式
$$
\sin(\alpha+\beta)=\sin\alpha\cos\beta+\cos\alpha\sin\beta
$$
可以得到反正弦函数的加法公式. 设
$$
\alpha=\arcsin x,\qquad\beta=\arcsin y,
\qquad -1\leqslant x,y\leqslant1.
$$
则
$$
\sin\alpha=x,\qquad \sin\beta=y,
$$
并且由于 $\alpha,\beta\in\bbrac{-\dfrac{\pi}{2},\dfrac{\pi}{2}}$,
根号前都应取正号:
$$
\cos\alpha=\sqrt{1-x^2},\qquad
\cos\beta=\sqrt{1-y^2}.
$$
因此
$$
\alpha+\beta
=\operatorname{Arcsin}\brac{x\sqrt{1-y^2}+y\sqrt{1-x^2}}.
$$
当 $\alpha+\beta$ 仍在
$\bbrac{-\dfrac{\pi}{2},\dfrac{\pi}{2}}$ 内时, 可将它写成主值形式
$$
\arcsin x+\arcsin y
=\arcsin\brac{x\sqrt{1-y^2}+y\sqrt{1-x^2}}.
$$
若 $x$ 与 $y$ 异号, 这个条件自动满足;
若它们同号, 这个条件等价于 $x^2+y^2\leqslant1$.

同样, 函数 $y=\cos x$ 的反函数
$$
y=\operatorname{Arccos}x,\qquad -1\leqslant x\leqslant1,
$$
也是无穷多值的. 附加条件
$$
0\leqslant\arccos x\leqslant\pi
$$
便分出它的单值主支. 由恒等式
$\sin x=\cos\brac{\dfrac{\pi}{2}-x}$ 可知
$$
\arcsin x+\arccos x=\frac{\pi}{2}.
$$
反余弦函数的全部数值可由主值表示为
$$
\operatorname{Arccos}x=2k\pi\pm\arccos x,\qquad k\in\Z.
$$

再考察函数 $y=\tan x$. 它对于除去
$x=\dfrac{(2k+1)\pi}{2}$ ($k\in\Z$) 以外的一切实数都有意义,
值域为 $(-\infty,+\infty)$. 对于任意一个函数值 $y$,
都有无穷多个 $x$ 与之对应, 因而反函数
$x=\operatorname{Arctan}y$ 是无穷多值的.
取正切函数在 $\brac{-\dfrac{\pi}{2},\dfrac{\pi}{2}}$ 上的一支,
便得到反正切函数的主值, 它满足
$$
-\frac{\pi}{2}<\arctan x<\frac{\pi}{2}.
$$
反正切函数的全部数值为
$$
\operatorname{Arctan}x=\arctan x+k\pi,\qquad k\in\Z.
$$
下图画出了多值函数 $y=\operatorname{Arctan}x$ 及其主值支的图像.
它就是把函数 $y=\tan x$ 的图像绕第一象限角的对角线旋转 180\textdegree 而得出的.

\begin{figure}[!h]
    \vspace{-1cm}
    \centering
    \begin{tikzpicture}[scale=0.8]
        \draw[-latex] (-6.5, 0) -- (6.5, 0) node[below,scale=0.8] {$x$};
        \draw[-latex] (0, -1.5) -- (0, 5) node[right,scale=0.8] {$y$};
        \node[below right,scale=0.8] (O) at (0, 0) {$O$};
        \draw[dashed] (-6, pi/2) -- (6, pi/2);

        \draw[domain=-6:6, smooth, thick] plot (\x, {rad(atan(\x))});
        \draw[dashed, domain=-6:6, smooth, thick] plot (\x, {rad(atan(\x))+ pi});

        \node[above right,scale=0.8] (A) at (0, pi/2) {$\frac{\pi}{2}$};
    \end{tikzpicture}
\end{figure}

正切的加法公式
$$
\tan(\alpha+\beta)
=\frac{\tan\alpha+\tan\beta}{1-\tan\alpha\tan\beta}
$$
同样可以用来推导反正切函数的加法公式. 设
$$
\alpha=\arctan x,\qquad\beta=\arctan y.
$$
当 $xy\ne1$ 时,
$$
\tan(\alpha+\beta)=\frac{x+y}{1-xy},
$$
因而
$$
\alpha+\beta
=\operatorname{Arctan}\frac{x+y}{1-xy}.
$$
若还要求 $-\dfrac{\pi}{2}<\alpha+\beta<\dfrac{\pi}{2}$,
也就是 $xy<1$, 便得到主值形式
$$
\arctan x+\arctan y
=\arctan\frac{x+y}{1-xy}.
$$

反正切函数与反正弦函数之间还有直接的关系:
$$
\begin{aligned}
\arctan x&=\arcsin\frac{x}{\sqrt{1+x^2}}
&&(-\infty<x<+\infty),\\
\arcsin x&=\arctan\frac{x}{\sqrt{1-x^2}}
&&(-1<x<1).
\end{aligned}
$$
例如, 令 $\alpha=\arctan x$, 则 $\tan\alpha=x$ 且
$-\dfrac{\pi}{2}<\alpha<\dfrac{\pi}{2}$. 于是
$$
\sin\alpha
=\frac{\tan\alpha}{\sqrt{1+\tan^2\alpha}}
=\frac{x}{\sqrt{1+x^2}}.
$$
由于根号前应取正号, 自然得到
$\alpha=\arcsin\dfrac{x}{\sqrt{1+x^2}}$.

再看多值函数 $\operatorname{Arccot}x$ ($-\infty<x<+\infty$).
它的主值由不等式
$$
0<\operatorname{arccot}x<\pi
$$
确定, 并且与反正切函数的主值满足
$$
\operatorname{arccot}x=\frac{\pi}{2}-\arctan x.
$$
反余切函数的其余数值为
$$
\operatorname{Arccot}x=\operatorname{arccot}x+k\pi,\qquad k\in\Z.
$$

至于反正割函数和反余割函数, 它们的定义域同为
$$
(-\infty,-1]\cup[1,+\infty),
$$
这里不再展开, 留给读者自行分析.

\subsection{函数的复合·总结}\label{subsec:51}
现在介绍函数复合的概念.
把一个函数作为另一个函数的自变量, 便得到一个\emph{复合函数}\index{fuhehanshu@复合函数}.
例如, 将函数 $y=\sin x$ 与 $z=\ln y$ 复合, 就得到函数
$z=\ln(\sin x)$;
类似地, 函数
$$
\sqrt{1-x^2},\qquad \arctan\frac{1}{x}
$$
也都是由较简单的函数复合而成的.

一般地, 设函数 $y=f(x)$ 在区域 $\X=\{x\}$ 内有定义,
并且它的值全部包含在区域 $\Y=\{y\}$ 内;
再设函数 $z=\varphi(y)$ 在区域 $\Y$ 内有定义.
这时, 变量 $z$ 以 $y$ 为媒介成为 $x$ 的函数, 记作
$$
z=\varphi(f(x)).
$$
对于 $\X$ 内任意给定的 $x$, 先按照 $f$ 所表示的规律求出相应的
$y=f(x)$, 再按照 $\varphi$ 所表示的规律求出相应的 $z=\varphi(y)$,
便得到与所选的 $x$ 对应的 $z$ 值.
所得的函数 $\varphi(f(x))$ 称为 $f$ 与 $\varphi$ 的复合函数,
这个过程称为函数的复合.

要求函数 $f(x)$ 的一切值都不超出函数 $\varphi(y)$ 的定义域,
这一条件十分重要; 若忽略它, 就可能得到没有意义的表达式.
例如, 设 $z=\ln y$, $y=\sin x$, 则只能考虑满足
$\sin x>0$ 的那些 $x$ 值, 否则表达式 $\ln(\sin x)$ 没有意义.

还应着重指出, 一种函数关系能否借助复合函数来表示,
并不取决于函数关系本身, 而只取决于表示这种关系的方法.
例如, 设
$$
z=\sqrt{1-y^2}\quad(-1\leqslant y\leqslant 1),
\qquad
y=\sin x\quad\left(-\frac{\pi}{2}\leqslant x\leqslant\frac{\pi}{2}\right),
$$
则
$$
z=\sqrt{1-\sin^2x}=\cos x.
$$
这里, 函数 $\cos x$ 便以复合函数的形式出现.

明确了函数复合的概念以后, 现在可以准确地说明分析学中所研究的
那些函数里最简单的一类: 首先是 \emref{subsec:48} 和
\emref{subsec:50} 中列举的七类初等函数; 其次是由它们经过四则运算
及有限次复合得到的函数.
这些函数都可以用初等函数的有限形式来表示, 有时也把它们统称为
\emph{初等函数}.

以后, 当我们掌握无穷级数、积分等更复杂的分析工具后,
还会引入另一些在分析学中同样重要、但已经超出初等函数范围的函数.
